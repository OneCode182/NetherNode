# Skills Index

Store only `.agents` helper specs for this harness.

- Add skill refs when a specialized subprocess is required.
- Keep each skill metadata in a dedicated folder under `.agents/skills/<skill-name>/`.
- Scope: execution helpers and reusable role behavior.
